# Local API credentials
FANTASYPROS_API_KEY=replace_me
SYNC_ALLOWED_ORIGINS=http://localhost:3000
# Optional token for non-browser clients. Send it as X-Sync-Token.
# SYNC_REQUEST_TOKEN=replace-with-a-random-secret
