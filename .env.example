# Local API credentials
FANTASYPROS_API_KEY=replace_me
# Server settings below are examples for shell exports, not .env.local loading.
# export SYNC_ALLOWED_ORIGINS=http://localhost:3000
# export DRAFT_SYNC_POLL_INTERVAL_MS=1000
# The server and Vite normally share an automatically generated .local/sync-token.
# Run pnpm sync:pair to pair the extension. Never put this token in VITE_* variables.
# Optional override: export the same random base64url token (43–128 characters)
# as SYNC_REQUEST_TOKEN to both services. API clients send X-Sync-Token.
